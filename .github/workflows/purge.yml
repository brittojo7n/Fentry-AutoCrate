name: Network Connectivity Test

on:
  workflow_dispatch:

jobs:
  test-connection:
    runs-on: ubuntu-latest # Or your self-hosted runner label
    steps:
      - name: Check connection to GitHub
        run: curl -v https://github.com
